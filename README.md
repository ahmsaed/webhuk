# TypeScript Starter

Strict TypeScript starter template.

## Commands

- `bun install` — install dependencies
- `bun run typecheck` — type-check without compiling
- `bun run build` — compile TypeScript
- `bun start` — run compiled code
